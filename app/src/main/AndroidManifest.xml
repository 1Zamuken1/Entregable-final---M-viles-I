<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IngresosGASTU">
        <activity
            android:name=".Activity_update_proyeccion"
            android:exported="false" />
        <activity
            android:name=".activity_main_proyeccion"
            android:exported="false" />
        <activity
            android:name=".UpdateIncomeActivity"
            android:exported="false" />
        <activity
            android:name=".AddIncomeActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />
        <activity
            android:name=".ProyeccionesActivity"
            android:exported="true"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".AddProyeccionActivity"
            android:exported="false" /> <!-- Nueva activity que será la de inicio -->
        <activity
            android:name=".SelectorActivity"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>